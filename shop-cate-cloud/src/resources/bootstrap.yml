mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
  global-config:
    db-config:
      id-type: auto
      table-prefix: t_
#  mapper-locations: classpath*:mappers/*.xml
server:
  port: 8083
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 10
      max-active: 10
      max-wait: 60000
      min-evictable-idle-time-millis: 300000
      min-idle: 5
      test-on-borrow: false
      test-on-return: false
      test-while-idle: true
      time-between-eviction-runs-millis: 60000
      validation-query: SELECT 1
      validation-query-timeout: 2000
    password: xue123.
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://192.168.10.1:3306/20082?serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useSSL=true
    username: root
  freemarker:
    checkTemplateLocation: false
  application:
    name: shop-cate-api
#  cloud:
#      consul:
#        discovery:
#          service-name: ${spring.application.name}
#        host: localhost
#        port: 8500
#        config:
#          enabled: true
#          format: yaml #设置配置值的格式
#          prefix: config #设置配置所在目录
#          profile-separator: ':' #设置配置的分隔符
#          data-key: data
#  profiles:
#    active: dev


#   config:
#        enabled: true #是否启用配置中心功能
#        format: yaml #设置配置值的格式
#        prefix: config #设置配置所在目录
#        profile-separator: ':' #设置配置的分隔符
#        data-key: data #配置key的名字，由于Consul是K/V存储，配置存储在对应K的V中
eureka:
  client:
    register-with-eureka: true #注册到Eureka的注册中心
    fetch-registry: true #获取注册实例列表
    service-url:
      defaultZone: http://replical1:8000/eureka/,http://replical2:8111/eureka/,http://replical3:8222/eureka/
